<h1>Write a review for <%= @desk.title %> Desk</h1>

<%= simple_form_for([@desk, @review]) do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
